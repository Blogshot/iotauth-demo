import { IIotAuth } from '../api/iotauth-api';
export declare class IotAuth implements IIotAuth {
    readonly iotaClient: any;
    private receiveSeed;
    private duration;
    constructor(seed?: string, duration?: number, node?: string);
    isTransactionValid(validationCode?: string): Promise<boolean>;
    getSeed(): Promise<string>;
    generateValidationCode(): Promise<string>;
    private getNewAddress(seed, options?);
    private isValidAddress(receiveAddress, index);
    private isValidTimestamp(timestamp);
    private generateNewSeed();
    private getAccountData(seed);
}
